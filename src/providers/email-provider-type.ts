/* v8 ignore start -- Enum not yet consumed; coverage will be added when provider selection is implemented. */
export enum EmailProviderType {
  ForwardEmail = 'forward-email',
  Imap = 'imap',
  MailTm = 'mail-tm'
}
/* v8 ignore stop */
